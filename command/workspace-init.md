---
description: Initialize project workspace with opencode tools
agent: build
model: glm-4.7
---

Run the workspace initialization script to set up this project with opencode:

```
~/.config/opencode/bin/workspace-init --force
```

After running, summarize what was set up (git, cass_memory, Beads, TLDR, Biome, Prettier, git hooks) and report any errors.
